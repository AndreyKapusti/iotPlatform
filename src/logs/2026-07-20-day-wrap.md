# 2026-07-20 — итог дня (закрытие сессии)

**Автор:** PM  
**Фаза:** MVP + UI-first polish  
**Стенд:** контейнеры остановлены (`docker compose --profile demo down`)

## Сделано за день

### Инфра / MVP
- Compose e2e: control + ingest + web + Timescale + Mosquitto + simulator
- Демо-логин `admin` / `admin123`, устройство `demo-sensor`

### UI продукт
- Светлая / тёмная тема (Настройки)
- Сворачиваемый сайдбар
- Несколько дашбордов на устройство, режимы **просмотр / редактирование**
- Адаптив под разные ширины экрана
- **UI-1** «Живой пульт»: toast, toggle (демо), online/last seen, empty states

### Редактор дашборда
- DnD виджетов; фикс выбора после drag; фикс вложенных графиков
- **Ресайз мышью** (E / S / SE) в edit-режиме
- **Цвет виджета** — именованные акценты с отдельными оттенками light/dark

### Docs / QA
- Backlog UI-first, чеклисты QA, design note по акцентам, сессионные логи

## Следующий день (план)

1. **UI-2** — пороги на number-виджетах, лента событий, период графика, дублирование дашборда  
2. По остатку — UI-3 онбординг  
3. **Не сейчас:** Arduino/ESP (отдельный день)

## Подводные камни дня

| Камень | Как жить дальше |
|--------|-----------------|
| Email `.local` режет EmailStr | Демо: `admin@example.com` |
| Один hex на обе темы ломает контраст | Только semantic accents |
| Handles ресайза только у selected | Сначала клик, потом тянуть угол |
| `docker compose down` без `--profile demo` оставляет simulator | Всегда down с тем же profile, что up |

## Решения / допущения
- Стратегия: UI-first на stub’ах; железо — отдельный день
- Violet в палитре виджетов не брали → `slate`
- Indicator ON остаётся success, не accent виджета

## Блокеры
- нет
